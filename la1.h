#include <stdlib.h>
#include "la1.c"

struct linkedNode {
	int val;
	struct linkedNode* nextNode = NULL;
}

linkedNode numGen(int num_values, int seed)

int numMeaner(linkedNode list)

void main()

